package service;

import domain.Exchange;

public class ExchangeDataLoaderService {
    public void loadData(Exchange exchange) {
        System.out.println("Loading initial data into the exchange...");
        // Load data from the database or a file
    }
}

